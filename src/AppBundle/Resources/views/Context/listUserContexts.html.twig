{% extends "@App/layout.html.twig" %}

{% block body %}
    <div>
        <h1 class="page-header">List of my contexts</h1>

        <ol class="breadcrumb">
            <li><a href="{{ path("homepage") }}">Home</a></li>
            <li class="active">My Contexts</li>
        </ol>

        <div class="table-responsive">
            <p>
                <a class="btn btn-primary" href="{{ path("create_new_context") }}">Create context</a>
                <a class="btn btn-primary" href="{{ path("import_context") }}">Import context</a>
            </p>

            {{ include("@App/Context/Helper/contextList.html.twig") }}
        </div>
    </div>
{% endblock %}
